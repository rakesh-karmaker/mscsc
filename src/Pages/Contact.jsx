import Contact from "../components/home-components/contact-components/Contact";

const ContactPage = () => {
  return (
    <main className="page-contact" style={{ marginTop: "var(--nav-height)" }}>
      <Contact />
    </main>
  );
};

export default ContactPage;
